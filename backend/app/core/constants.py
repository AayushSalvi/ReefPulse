"""Stable ids for seeded demo data."""

# Stable UUID for the seeded demo account (`user_id=demo` query maps here).
DEMO_USER_UUID = "00000000-0000-4000-8000-000000000001"

# Seeded demo login (development only; change in production).
# Use a TLD email-validator accepts (.local is rejected).
DEMO_USER_EMAIL = "demo@reefpulse.dev"
DEMO_USER_PASSWORD = "demo123"
